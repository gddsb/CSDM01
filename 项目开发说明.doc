# 奶粉罐生产管理系统（Milk Can MES）

> 项目开发说明文档
> 版本：前端 v4.3.0 / 后端 v1.0.0
> 文档生成日期：2026-07-05

---

## 目录

- [1. 项目概述](#1-项目概述)
  - [1.1 项目背景](#11-项目背景)
  - [1.2 系统定位](#12-系统定位)
  - [1.3 核心业务范围](#13-核心业务范围)
- [2. 技术栈](#2-技术栈)
  - [2.1 前端技术栈](#21-前端技术栈)
  - [2.2 后端技术栈](#22-后端技术栈)
  - [2.3 开发与运行环境](#23-开发与运行环境)
- [3. 项目结构](#3-项目结构)
  - [3.1 整体目录结构](#31-整体目录结构)
  - [3.2 前端目录结构](#32-前端目录结构)
  - [3.3 后端目录结构](#33-后端目录结构)
- [4. 数据库设计](#4-数据库设计)
  - [4.1 数据库选型与配置](#41-数据库选型与配置)
  - [4.2 数据表清单](#42-数据表清单)
  - [4.3 核心表结构说明](#43-核心表结构说明)
  - [4.4 模型关联关系](#44-模型关联关系)
  - [4.5 状态字段映射设计](#45-状态字段映射设计)
- [5. 后端架构](#5-后端架构)
  - [5.1 应用入口](#51-应用入口)
  - [5.2 中间件](#52-中间件)
  - [5.3 路由设计](#53-路由设计)
  - [5.4 控制器层](#54-控制器层)
  - [5.5 统一响应格式](#55-统一响应格式)
  - [5.6 认证机制](#56-认证机制)
  - [5.7 操作日志](#57-操作日志)
- [6. 前端架构](#6-前端架构)
  - [6.1 应用入口与路由](#61-应用入口与路由)
  - [6.2 全局状态管理](#62-全局状态管理)
  - [6.3 HTTP 请求封装](#63-http-请求封装)
  - [6.4 主题系统](#64-主题系统)
  - [6.5 布局组件](#65-布局组件)
  - [6.5.1 三段式页面设计规范](#651-三段式页面设计规范)
  - [6.6 Vite 配置与代理](#66-vite-配置与代理)
- [7. 业务模块说明](#7-业务模块说明)
  - [7.1 基础数据模块](#71-基础数据模块)
  - [7.2 生产管理模块](#72-生产管理模块)
  - [7.3 质量管理模块](#73-质量管理模块)
  - [7.4 设备管理模块](#74-设备管理模块)
  - [7.5 报表中心模块](#75-报表中心模块)
  - [7.6 大屏展示模块](#76-大屏展示模块)
  - [7.7 系统管理模块](#77-系统管理模块)
- [8. API 接口文档](#8-api-接口文档)
  - [8.1 接口约定](#81-接口约定)
  - [8.2 认证接口](#82-认证接口)
  - [8.3 系统管理接口](#83-系统管理接口)
  - [8.4 基础数据接口](#84-基础数据接口)
  - [8.5 生产管理接口](#85-生产管理接口)
- [9. 业务流程](#9-业务流程)
  - [9.1 生产订单业务流程](#91-生产订单业务流程)
  - [9.2 工单业务流程](#92-工单业务流程)
  - [9.3 工序报工流程](#93-工序报工流程)
  - [9.4 用户认证流程](#94-用户认证流程)
- [10. 部署说明](#10-部署说明)
  - [10.1 开发环境部署](#101-开发环境部署)
  - [10.2 生产环境构建](#102-生产环境构建)
  - [10.3 数据库初始化与种子数据](#103-数据库初始化与种子数据)
- [11. 开发规范与注意事项](#11-开发规范与注意事项)
  - [11.1 命名规范](#111-命名规范)
  - [11.2 状态字段处理规范](#112-状态字段处理规范)
  - [11.3 前后端字段映射](#113-前后端字段映射)
  - [11.4 已知问题与解决方案](#114-已知问题与解决方案)

---

## 1. 项目概述

### 1.1 项目背景

本项目为**奶粉罐生产管理系统**（Milk Can MES），面向奶粉罐制造企业的车间执行层，覆盖从生产订单下达、工单派工、工序报工到质量检验、设备管理、数据看板的完整生产管理闭环。

### 1.2 系统定位

- **类型**：MES（Manufacturing Execution System，制造执行系统）
- **目标行业**：金属包装 / 奶粉罐制造
- **用户角色**：生产计划员、车间班组长、质检员、设备管理员、系统管理员
- **部署形态**：B/S 架构，支持内网部署

### 1.3 核心业务范围

| 业务域 | 主要功能 |
|--------|---------|
| 基础数据 | 料品档案、产线、工序、设备、不良分类 |
| 生产管理 | 生产订单、工单、工序报工、人员投入、异常记录 |
| 质量管理 | 检验标准、来料/工序/成品/微生物/环境检验、客诉管理 |
| 设备管理 | 设备档案、点检记录、维修保养、OEE 分析 |
| 报表中心 | 日报、月报、效率报表、生产报表、质量报表、异常报表 |
| 大屏展示 | 生产大屏、管理大屏、质量大屏 |
| 系统管理 | 用户、角色、权限、数据字典、操作日志、系统配置 |

---

## 2. 技术栈

### 2.1 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | ^18.3.1 | UI 框架 |
| React Router DOM | ^6.26.2 | 路由管理 |
| Ant Design | ^5.21.0 | UI 组件库 |
| @ant-design/icons | ^5.5.1 | 图标库 |
| Axios | ^1.18.1 | HTTP 请求 |
| dayjs | ^1.11.13 | 日期处理 |
| ECharts | ^6.1.0 | 图表与大屏可视化 |
| Vite | ^5.4.8 | 构建工具 |
| @vitejs/plugin-react | ^4.3.1 | Vite React 插件 |

### 2.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | ≥ 18 | 运行时 |
| Express | ^4.21.0 | Web 框架 |
| Sequelize | ^6.37.3 | ORM |
| SQLite3 | ^5.1.7 | 开发数据库 |
| MySQL2 | ^3.11.0 | 生产数据库驱动 |
| JSON Web Token | ^9.0.2 | 身份认证 |
| bcryptjs | ^2.4.3 | 密码加密 |
| CORS | ^2.8.5 | 跨域支持 |
| Morgan | ^1.10.0 | HTTP 日志 |
| dotenv | ^16.4.5 | 环境变量管理 |

### 2.3 开发与运行环境

- **操作系统**：Linux / Windows / macOS
- **Node.js**：≥ 18.x
- **包管理**：npm
- **开发端口**：前端 5173，后端 3001
- **数据库**：开发环境 SQLite（无需安装），生产环境 MySQL

---

## 3. 项目结构

### 3.1 整体目录结构

```
CSDM01/
├── src/                     # 前端源码
├── server/                  # 后端源码
├── public/                  # 前端静态资源
├── index.html               # 前端入口 HTML
├── vite.config.js           # Vite 配置
├── package.json             # 前端依赖
└── README.md                # 项目说明
```

### 3.2 前端目录结构

```
src/
├── assets/                  # 静态资源（logo 等）
├── components/              # 通用组件
│   └── ThreeSectionPage.jsx # 三段式页面布局组件
├── contexts/                # 全局上下文
│   └── AppContext.jsx       # 应用上下文（用户/主题/登录）
├── layouts/                 # 布局组件
│   └── MainLayout.jsx       # 主布局（侧边栏+顶栏+内容区）
├── mock/                    # Mock 数据
│   └── data.js
├── pages/                   # 页面组件
│   ├── Dashboard.jsx        # 仪表盘
│   ├── Login.jsx            # 登录页
│   ├── basic/               # 基础数据
│   │   ├── DefectManagement.jsx
│   │   ├── DeviceManagement.jsx
│   │   ├── MaterialManagement.jsx
│   │   ├── ProcessManagement.jsx
│   │   └── ProductionLine.jsx
│   ├── bigscreen/           # 大屏展示
│   │   ├── ManagementBigScreen.jsx
│   │   ├── ProductionBigScreen.jsx
│   │   └── QualityBigScreen.jsx
│   ├── device/              # 设备管理
│   │   ├── CheckRecord.jsx
│   │   ├── DeviceManagement.jsx
│   │   ├── DeviceOEE.jsx
│   │   └── Maintenance.jsx
│   ├── production/          # 生产管理
│   │   ├── ExceptionRecord.jsx
│   │   ├── ManpowerRecord.jsx
│   │   ├── OrderManagement.jsx
│   │   ├── ProcessReporting.jsx
│   │   ├── ProductionReportByOrder.jsx
│   │   └── WorkOrderManagement.jsx
│   ├── quality/             # 质量管理
│   │   ├── ComplaintManagement.jsx
│   │   ├── EnvironmentInspection.jsx
│   │   ├── FinishedInspection.jsx
│   │   ├── IncomingInspection.jsx
│   │   ├── InspectionStandard.jsx
│   │   ├── InstrumentManagement.jsx
│   │   ├── MicrobeInspection.jsx
│   │   ├── ProcessInspection.jsx
│   │   └── SupplierComplaint.jsx
│   ├── report/              # 报表中心
│   │   ├── DailyReport.jsx
│   │   ├── EfficiencyReport.jsx
│   │   ├── ExceptionReport.jsx
│   │   ├── MonthlyReport.jsx
│   │   ├── ProductionReport.jsx
│   │   └── QualityReport.jsx
│   └── system/              # 系统管理
│       ├── DataDictionary.jsx
│       ├── OperationLogs.jsx
│       ├── RoleManagement.jsx
│       ├── SystemConfig.jsx
│       └── UserManagement.jsx
├── styles/                  # 全局样式
│   ├── bigscreen.css
│   └── global.css
├── themes/                  # 主题配置
│   └── index.js
├── utils/                   # 工具函数
│   └── api.js               # Axios 实例与拦截器
└── main.jsx                 # 应用入口
```

### 3.3 后端目录结构

```
server/
├── data/                    # SQLite 数据库文件目录
│   └── milk_can_mes.sqlite
├── src/
│   ├── config/              # 配置
│   │   └── database.js      # 数据库连接配置
│   ├── controllers/         # 控制器
│   │   ├── AuthController.js
│   │   ├── DefectTypeController.js
│   │   ├── DeviceController.js
│   │   ├── ExceptionRecordController.js
│   │   ├── ManpowerRecordController.js
│   │   ├── MaterialController.js
│   │   ├── OperationLogController.js
│   │   ├── OrderController.js
│   │   ├── ProcessController.js
│   │   ├── ProcessReportController.js
│   │   ├── ProductionLineController.js
│   │   ├── RoleController.js
│   │   ├── SystemConfigController.js
│   │   ├── UserController.js
│   │   ├── WorkOrderController.js
│   │   └── DataDictionaryController.js
│   ├── middleware/          # 中间件
│   │   └── auth.js          # JWT 认证、权限校验、操作日志
│   ├── models/              # 数据模型
│   │   ├── index.js         # 模型聚合与关联关系
│   │   ├── User.js
│   │   ├── Role.js
│   │   ├── Permission.js
│   │   ├── RolePermission.js
│   │   ├── OperationLog.js
│   │   ├── Material.js
│   │   ├── ProductionLine.js
│   │   ├── Process.js
│   │   ├── Device.js
│   │   ├── DefectType.js
│   │   ├── Order.js
│   │   ├── WorkOrder.js
│   │   ├── ProcessReport.js
│   │   ├── ManpowerRecord.js
│   │   ├── ExceptionRecord.js
│   │   └── SystemConfig.js
│   ├── routes/              # 路由
│   │   ├── index.js         # 路由聚合
│   │   ├── auth.js          # 认证路由
│   │   ├── basic.js         # 基础数据路由
│   │   ├── production.js    # 生产管理路由
│   │   └── system.js        # 系统管理路由
│   ├── utils/               # 工具
│   │   ├── jwt.js           # JWT 生成与验证
│   │   ├── response.js      # 统一响应封装
│   │   └── statusMap.js     # 状态映射工具
│   ├── app.js               # 应用入口
│   ├── seed.js              # 种子数据
│   ├── clean-init.js        # 清空业务数据
│   └── import-materials.js  # 料品导入脚本
└── package.json
```

---

## 4. 数据库设计

### 4.1 数据库选型与配置

数据库通过环境变量 `DB_DIALECT` 切换：

```javascript
// server/src/config/database.js
const dialect = process.env.DB_DIALECT || 'sqlite'

if (dialect === 'mysql') {
  // 生产环境：MySQL
  // 连接参数：DB_HOST / DB_PORT / DB_NAME / DB_USER / DB_PASSWORD
} else {
  // 开发环境：SQLite，存储于 ./data/milk_can_mes.sqlite
}
```

**统一约定**：
- 所有表启用 `timestamps: true`，自动生成 `created_at` / `updated_at`
- 字段命名采用 `underscored: true`（蛇形命名）

### 4.2 数据表清单

| 序号 | 表名 | 模型 | 说明 |
|------|------|------|------|
| 1 | sys_user | User | 系统用户 |
| 2 | sys_role | Role | 角色 |
| 3 | sys_permission | Permission | 权限项 |
| 4 | sys_role_permission | RolePermission | 角色-权限关联 |
| 5 | sys_operation_log | OperationLog | 操作日志 |
| 6 | sys_config | SystemConfig | 系统配置 |
| 7 | bas_material | Material | 料品档案 |
| 8 | bas_production_line | ProductionLine | 产线 |
| 9 | bas_process | Process | 工序 |
| 10 | bas_device | Device | 设备 |
| 11 | bas_defect_type | DefectType | 不良分类 |
| 12 | production_order | Order | 生产订单 |
| 13 | production_work_order | WorkOrder | 工单 |
| 14 | production_process_report | ProcessReport | 工序报工 |
| 15 | production_manpower_record | ManpowerRecord | 人员投入记录 |
| 16 | production_exception_record | ExceptionRecord | 异常记录 |

### 4.3 核心表结构说明

#### 4.3.1 sys_user（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | INTEGER PK | 用户 ID（自增） |
| username | VARCHAR(50) | 登录账号（唯一） |
| password | VARCHAR(100) | 密码（bcrypt 加密） |
| real_name | VARCHAR(50) | 真实姓名 |
| employee_no | VARCHAR(50) | 工号 |
| department | VARCHAR(50) | 部门 |
| role_id | INTEGER | 角色 ID |
| phone | VARCHAR(20) | 电话 |
| email | VARCHAR(100) | 邮箱 |
| status | TINYINT | 状态：1启用 / 0禁用 |
| last_login_time | DATETIME | 最后登录时间 |

#### 4.3.2 bas_material（料品档案）

| 字段 | 类型 | 说明 |
|------|------|------|
| material_id | UUID PK | 料品 ID |
| category_name | VARCHAR(50) | 类别名称 |
| material_code | VARCHAR(50) | 料号（唯一） |
| material_name | VARCHAR(200) | 料品名称 |
| specification | VARCHAR(200) | 规格 |
| unit_name | VARCHAR(50) | 单位 |
| film_no | VARCHAR(50) | 菲林编号 |
| version_no | VARCHAR(50) | 版本号 |
| cutting_size | VARCHAR(50) | 裁切尺寸 |
| printing_process | VARCHAR(50) | 印刷工艺 |
| color_separation | VARCHAR(50) | 分色 |
| blanking_diameter | DECIMAL(11,2) | 落料直径 |
| material_thickness | DECIMAL(11,2) | 材料厚度 |
| material_width | DECIMAL(11,2) | 材料宽度 |
| material_height | DECIMAL(11,2) | 材料高度 |
| scrap_weight | DECIMAL(11,2) | 边料重量 |
| unit_weight | DECIMAL(11,2) | 单位重量 |
| unit_volume | DECIMAL(11,2) | 单位体积 |
| weight_unit | VARCHAR(20) | 重量单位 |
| volume_unit | VARCHAR(20) | 体积单位 |
| inventory_category | VARCHAR(20) | 存货类别 |
| unit_code | VARCHAR(20) | 单位编码 |
| is_active | BOOLEAN | 是否启用 |
| effective_date | DATETIME | 生效日期 |
| expiry_date | DATETIME | 失效日期 |

#### 4.3.3 production_order（生产订单）

| 字段 | 类型 | 说明 |
|------|------|------|
| order_id | INTEGER PK | 订单 ID（自增） |
| order_no | VARCHAR(50) | 订单号（唯一，自动生成） |
| material_id | UUID | 料品 ID |
| material_code | VARCHAR(50) | 料号（冗余） |
| material_name | VARCHAR(100) | 料品名称（冗余） |
| specification | VARCHAR(200) | 规格（冗余） |
| film_version | VARCHAR(50) | 菲林编号（来自 material.film_no） |
| version_no | VARCHAR(50) | 版本号 |
| planned_qty | DECIMAL(12,2) | 计划数量 |
| finished_qty | DECIMAL(12,2) | 完工数量 |
| plan_start_time | DATETIME | 计划开始时间 |
| plan_end_time | DATETIME | 计划结束时间 |
| status | TINYINT | 状态：0开立 / 1已下达 / 2已关闭 |
| release_time | DATETIME | 下达时间 |
| close_time | DATETIME | 关闭时间 |
| created_by | VARCHAR(50) | 创建人 |
| remark | VARCHAR(500) | 备注 |

**订单号生成规则**：`MO-16` + `YYMMDD` + `3位序号`
示例：`MO-16260705001`

#### 4.3.4 production_work_order（工单）

| 字段 | 类型 | 说明 |
|------|------|------|
| work_order_id | INTEGER PK | 工单 ID |
| work_order_no | VARCHAR(50) | 工单号（唯一） |
| order_id | INTEGER | 关联订单 ID |
| order_no | VARCHAR(50) | 订单号（冗余） |
| line_id | INTEGER | 产线 ID |
| line_name | VARCHAR(100) | 产线名称 |
| material_id | UUID | 料品 ID |
| material_name | VARCHAR(100) | 料品名称 |
| target_qty | DECIMAL(12,2) | 目标数量 |
| finished_qty | DECIMAL(12,2) | 完工数量 |
| start_time | DATETIME | 开始时间 |
| finish_time | DATETIME | 完成时间 |
| total_hours | DECIMAL(10,2) | 总工时 |
| effective_hours | DECIMAL(10,2) | 有效工时 |
| labor_hours | DECIMAL(10,2) | 人工工时 |
| status | TINYINT | 状态：0开立 / 1开工 / 2关闭 / 3完工 |
| created_by | VARCHAR(50) | 创建人 |

### 4.4 模型关联关系

```
User (N) ──── (1) Role
                 │
                 └── (N) ←──→ (N) Permission  [通过 RolePermission]

Order (1) ──── (N) WorkOrder
  │                  │
  │                  ├── (N) ProcessReport
  │                  ├── (N) ManpowerRecord
  │                  └── (N) ExceptionRecord
  │
  └── (N) ExceptionRecord
```

代码定义位于 [server/src/models/index.js](file:///workspace/server/src/models/index.js)：

```javascript
User.belongsTo(Role, { foreignKey: 'role_id', as: 'role' })
Order.hasMany(WorkOrder, { foreignKey: 'order_id', as: 'work_orders' })
WorkOrder.hasMany(ProcessReport, { foreignKey: 'work_order_id', as: 'process_reports' })
WorkOrder.hasMany(ManpowerRecord, { foreignKey: 'work_order_id', as: 'manpower_records' })
WorkOrder.hasMany(ExceptionRecord, { foreignKey: 'work_order_id', as: 'exception_records' })
Order.hasMany(ExceptionRecord, { foreignKey: 'order_id', as: 'exception_records' })
Role.belongsToMany(Permission, { through: RolePermission, foreignKey: 'role_id', otherKey: 'perm_id', as: 'permissions' })
```

### 4.5 状态字段映射设计

为兼顾前端展示中文与后端存储数字，状态字段统一使用 Sequelize 的 getter/setter 进行双向映射。

**订单状态**（[server/src/models/Order.js](file:///workspace/server/src/models/Order.js)）：

```javascript
status: {
  type: DataTypes.TINYINT,
  defaultValue: 0,
  get() {
    const val = this.getDataValue('status')
    const map = { 0: '开立', 1: '已下达', 2: '已关闭' }
    return map[val] !== undefined ? map[val] : val
  },
  set(val) {
    if (typeof val === 'string') {
      const map = { '开立': 0, '已下达': 1, '已关闭': 2 }
      this.setDataValue('status', map[val] !== undefined ? map[val] : 0)
    } else {
      this.setDataValue('status', val)
    }
  },
}
```

**通用状态映射工具**（[server/src/utils/statusMap.js](file:///workspace/server/src/utils/statusMap.js)）：

| 类型 | 状态映射 |
|------|---------|
| common | 启用=1, 禁用=0 |
| material | 启用=1, 试产=2, 停产=0 |
| order | 开立=0, 已下达=1, 已关闭=2 |
| workOrder | 开立=0, 开工=1, 关闭=2, 完工=3 |
| device | 运行=1, 停用=0, 维修=2 |
| line | 运行中=1, 停用=0 |

---

## 5. 后端架构

### 5.1 应用入口

入口文件：[server/src/app.js](file:///workspace/server/src/app.js)

启动流程：
1. 加载 `.env` 环境变量
2. 创建 Express 应用
3. `initDatabase()`：同步数据库表 → 初始化系统配置 → 初始化默认权限
4. 注册中间件：CORS、JSON 解析、URL 解析、Morgan 日志
5. 健康检查接口 `GET /api/health`
6. 挂载业务路由 `/api`
7. 404 处理与全局错误处理
8. 监听端口（默认 3001）

```javascript
const PORT = process.env.PORT || 3001
app.use(cors())
app.use(express.json())
app.use(express.urlencoded({ extended: true }))
app.use(morgan('dev'))
app.use('/api', routes)
app.listen(PORT, () => { /* ... */ })
```

### 5.2 中间件

中间件文件：[server/src/middleware/auth.js](file:///workspace/server/src/middleware/auth.js)

| 中间件 | 功能 |
|--------|------|
| `authRequired` | JWT 认证，从 `Authorization: Bearer xxx` 解析并验证 token，挂载 `req.user` |
| `permissionRequired(permCode)` | 权限校验（当前简化为登录即可） |
| `logOperation(module)` | 操作日志记录，响应结束后异步写入 `sys_operation_log` |

**操作日志记录字段**：user_id、username、module、operation、method、params、ip、status

### 5.3 路由设计

路由聚合：[server/src/routes/index.js](file:///workspace/server/src/routes/index.js)

```javascript
router.use('/auth', authRoutes)         // /api/auth
router.use('/system', systemRoutes)     // /api/system
router.use('/basic', basicRoutes)       // /api/basic
router.use('/production', productionRoutes) // /api/production
```

| 路由文件 | 前缀 | 主要资源 |
|---------|------|---------|
| auth.js | /api/auth | login, profile, logout |
| system.js | /api/system | users, roles, permissions, logs, config, data-dictionary |
| basic.js | /api/basic | materials, production-lines, processes, devices, defect-types |
| production.js | /api/production | orders, work-orders, process-reports, manpower-records, exceptions |

### 5.4 控制器层

控制器统一遵循以下模式：

```javascript
export const list = async (req, res) => {
  try {
    // 1. 解析查询参数
    // 2. 构造 where 条件
    // 3. 调用 Model.findAndCountAll
    // 4. 返回 success(res, list, '查询成功', count)
  } catch (err) {
    return fail(res, '服务器错误', 500)
  }
}
```

主要控制器：

| 控制器 | 主要方法 |
|--------|---------|
| AuthController | login, profile, logout |
| UserController | list, detail, create, update, remove, toggle |
| RoleController | list, create, update, remove, listPermissions, getRolePermissions, assignPermissions |
| MaterialController | list, detail, create, update, remove |
| OrderController | list, detail, create, update, remove, release, close |
| WorkOrderController | list, detail, create, update, remove, start, finish, close |
| ProcessReportController | list, create |
| ManpowerRecordController | list, create |
| ExceptionRecordController | list, create |
| DataDictionaryController | list, detail（动态读取表结构） |
| SystemConfigController | getConfig, saveConfig, initDefaultConfigs |
| OperationLogController | list |

### 5.5 统一响应格式

工具文件：[server/src/utils/response.js](file:///workspace/server/src/utils/response.js)

```javascript
// 成功响应
{ success: true, message: '操作成功', data: {...}, total: 100 }

// 失败响应
{ success: false, message: '错误描述' }
```

### 5.6 认证机制

- **登录**：`POST /api/auth/login`，校验账号密码后返回 JWT token 与用户信息
- **Token 存储**：前端存于 `localStorage.mes_token`
- **请求携带**：Axios 拦截器自动添加 `Authorization: Bearer <token>`
- **验证**：`authRequired` 中间件解析并校验 token
- **失效处理**：响应拦截器收到 401 时清除 token 并跳转登录页

### 5.7 操作日志

通过 `logOperation(module)` 中间件在响应结束后异步记录，覆盖以下模块：
- 用户管理、角色管理、角色权限分配
- 系统配置
- 料品档案、产线、工序、设备、不良分类
- 生产订单、工单、工序报工、人员投入、异常记录

---

## 6. 前端架构

### 6.1 应用入口与路由

入口文件：[src/main.jsx](file:///workspace/src/main.jsx)

**核心特性**：
- `ConfigProvider` 注入 Ant Design 中文 locale 与主题 token（`colorPrimary: #2196F3`）
- `BrowserRouter` 包裹整个应用
- `AppProvider` 提供全局上下文
- `ProtectedRoute` 实现路由守卫：未登录跳转 `/login`
- 大屏路由独立全屏渲染（不套主布局）

**路由清单**（部分）：

| 路径 | 组件 | 说明 |
|------|------|------|
| /login | Login | 登录 |
| /dashboard | Dashboard | 仪表盘 |
| /system/users | UserManagement | 用户管理 |
| /system/roles | RoleManagement | 角色管理 |
| /system/dictionary | DataDictionary | 数据字典 |
| /system/config | SystemConfig | 系统配置 |
| /system/logs | OperationLogs | 操作日志 |
| /basic/materials | MaterialManagement | 料品档案 |
| /basic/lines | ProductionLine | 产线 |
| /basic/processes | ProcessManagement | 工序 |
| /basic/devices | DeviceManagement | 设备 |
| /basic/defects | DefectManagement | 不良分类 |
| /production/orders | OrderManagement | 生产订单 |
| /production/workorders | WorkOrderManagement | 工单 |
| /production/reporting | ProcessReporting | 工序报工 |
| /production/manpower | ManpowerRecord | 人员投入 |
| /production/exceptions | ExceptionRecord | 异常记录 |
| /quality/* | 质量管理各页面 | 检验标准/来料/工序/成品/微生物/环境/客诉/仪器 |
| /device/* | 设备管理各页面 | 档案/点检/维修/OEE |
| /report/* | 报表各页面 | 日报/月报/效率/生产/质量/异常 |
| /bigscreen/* | 大屏页面 | 生产/管理/质量大屏 |

### 6.2 全局状态管理

上下文文件：[src/contexts/AppContext.jsx](file:///workspace/src/contexts/AppContext.jsx)

**提供的能力**：
- `currentUser` / `login` / `logout` / `updateUser`
- `themeKey` / `changeTheme`
- `initialized`（应用初始化完成标志）

**持久化**：
- `mes_token`：JWT token
- `mes_user`：用户信息 JSON
- `mes_theme`：主题 key

### 6.3 HTTP 请求封装

工具文件：[src/utils/api.js](file:///workspace/src/utils/api.js)

```javascript
const api = axios.create({
  baseURL: '/api',
  timeout: 15000,
})

// 请求拦截器：自动添加 token
api.interceptors.request.use(config => {
  const token = localStorage.getItem('mes_token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截器：统一错误处理
// - 401：清除登录信息并跳转 /login
// - 其他：提取 message 抛出
```

### 6.4 主题系统

主题文件：[src/themes/index.js](file:///workspace/src/themes/index.js)

支持多主题切换，包含 `pureMilk`（纯白乳白主题）与 `darkFactory`（工厂暗色主题）。暗色主题通过 Ant Design `darkAlgorithm` 实现。

### 6.5 布局组件

- **MainLayout**：主布局，包含侧边栏菜单、顶栏（用户信息/主题切换/退出）、内容区
- **ThreeSectionPage**：三段式页面通用组件，统一页面布局（页面信息区 + 筛选功能区 + 列表区）

### 6.5.1 三段式页面设计规范

本系统所有业务列表页统一采用三段式布局，由 [src/components/ThreeSectionPage.jsx](file:///workspace/src/components/ThreeSectionPage.jsx) 实现，样式定义于 [src/styles/global.css](file:///workspace/src/styles/global.css)。

#### 一、区域划分与规格

| 区域 | 高度占比 | 内容组成 | 背景色 | 行间距规则 |
|------|---------|---------|--------|-----------|
| 上部：页面信息区 | 15%-20% | 页面标题、面包屑导航、快速筛选（今天/本周/本月/本年）、统计数据卡片（2-5个指标）、主要操作按钮（新增/导出/配置） | 主背景色 | 常规间距 |
| 中部：筛选功能区 | 10%-15% | 查询条件输入框、下拉选择器、日期范围选择、查询/重置按钮、展开/收起切换 | 卡片背景色，可折叠 | 小行间距（4-8px） |
| 下部：列表区 | 65%-75% | 数据表格、分页器、总计记录数 | 卡片背景色 | 小行间距（4-8px） |

#### 二、页面设计说明

1. **上部页面信息区**保持常规间距，以维持页面整体的呼吸感和层次感。设有"今天、本周、本月、本年"快速筛选统计按钮，默认选中"本月"。
2. **中部筛选功能区**和**下部列表区**采用小行间距，使数据密集型操作区域布局更加紧凑，提升屏幕空间利用率。中部筛选区域默认所有功能保持在一行。
3. 具体表现为：表单控件之间、表格行之间的垂直间距缩小（建议 4px-8px），区域内部的 padding 值相应减小，但需保持足够的可点击区域以确保操作准确性。
4. **下部列表区**默认根据列内容保持完整显示自动列宽：
   - 料品名称列宽默认最小 200px，自动换行
   - 操作列根据操作功能数量设置列宽：1~3 个功能 80px，4 个功能 100px（通过 `getActionColumnWidth(count)` 工具函数计算）

#### 三、交互特性

| 特性 | 说明 |
|------|------|
| 快速筛选联动 | 上部统计卡片数据跟随"今天/本周/本月/本年"快速筛选条件联动更新 |
| 筛选区折叠 | 中部筛选区支持"展开更多/收起"切换，默认显示常用筛选条件在一行，点击展开显示全部 |
| 内容变化触发查询 | 筛选区支持内容变化触发查询（通过 `onFilterChange` 回调） |
| 表格列操作 | 列表区表格支持列宽拖拽、列排序、列显隐设置 |
| 统计卡片独立 | 上部统计卡片数据**不**跟随中部筛选条件联动更新，仅跟随快速筛选联动 |

#### 四、组件 API

```jsx
<ThreeSectionPage
  title="页面标题"
  breadcrumbs="模块 / 子菜单"
  stats={[{ label, value, icon, color }]}        // 统计卡片 2-5 个
  filters={[{ type, field, placeholder, value, onChange, options, col }]}  // 筛选项
  actions={<ActionButtons onAdd={...} />}        // 操作按钮
  table={<Table ... />}                          // 数据表格
  onSearch={handleSearch}
  onReset={handleReset}
  // 快速筛选
  quickFilter={true}                             // 默认 true，显示今天/本周/本月/本年
  defaultQuickFilter="month"                     // 默认 'month'
  onQuickFilterChange={(val) => { /* 重新统计 */ }}
  // 筛选区折叠
  collapsible={true}                             // 默认 true
  defaultCollapsed={false}
  // 内容变化触发查询
  onFilterChange={(field, value) => { /* 实时查询 */ }}
/>
```

#### 五、辅助工具

| 工具 | 用途 |
|------|------|
| `ActionButtons` | 通用操作按钮组（新增/导出/额外按钮） |
| `ColumnSettings` | 表格列显隐设置下拉面板 |
| `getActionColumnWidth(count)` | 计算操作列宽度：1-3 功能→80px，4 功能→100px |
| `materialNameColumn` | 料品名称列默认配置：最小 200px，自动换行 |

#### 六、CSS 规范要点

```css
/* 区域间距与高度比例 */
.three-section-page { display: flex; flex-direction: column; gap: 8px; min-height: calc(100vh - 110px); }
.section-info    { background: var(--bg-main);  padding: 14px 20px; flex: 0 0 auto; }   /* 上部 */
.section-filter  { background: var(--bg-card);  padding: 8px 12px;  flex: 0 0 auto; }   /* 中部 */
.section-table   { background: var(--bg-card);  padding: 8px 12px;  flex: 1 1 auto; }   /* 下部 */

/* 小行间距：4px */
.section-filter .ant-form-item { margin-bottom: 4px !important; }
.section-filter .ant-row       { row-gap: 4px !important; }
.section-table .ant-table-small .ant-table-cell { padding: 4px 8px !important; }

/* 筛选区折叠动画 */
.section-filter-body.filter-collapsed { max-height: 0; opacity: 0; overflow: hidden; }
.section-filter-body:not(.filter-collapsed) { max-height: 500px; opacity: 1; }
```

#### 七、分页规范

下部列表区默认每页显示 **30 条**记录，通过 Table 的 `pagination` 配置实现：

```jsx
pagination={{
  current: query.page,
  pageSize: 30,            // 默认每页 30 条
  total,
  showSizeChanger: true,
  showTotal: t => `共 ${t} 条`,
}}
```


### 6.6 Vite 配置与代理

配置文件：[vite.config.js](file:///workspace/vite.config.js)

```javascript
export default defineConfig({
  plugins: [react()],
  server: {
    host: '0.0.0.0',
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3001',
        changeOrigin: true,
      },
    },
  },
})
```

开发环境下，前端 `/api` 请求被代理至后端 `http://localhost:3001`，解决跨域问题。

---

## 7. 业务模块说明

### 7.1 基础数据模块

| 页面 | 路径 | 功能 |
|------|------|------|
| 料品档案 | /basic/materials | 维护 24 个字段的料品信息，支持模糊搜索、分页、启用/试产/停产状态管理 |
| 产线 | /basic/lines | 生产线基础信息，状态：运行中/停用 |
| 工序 | /basic/processes | 工序定义与顺序维护 |
| 设备 | /basic/devices | 设备档案，状态：运行/停用/维修 |
| 不良分类 | /basic/defects | 缺陷类型字典 |

### 7.2 生产管理模块

#### 7.2.1 生产订单（OrderManagement）

**核心功能**：
- 订单列表：支持订单号/料品名称关键词搜索、料号模糊匹配、状态筛选、计划日期范围筛选
- 新增订单：选择料品后自动冗余料品信息（料号/名称/规格/菲林编号/版本号），支持备注输入（最多500字）
- 订单详情：抽屉展示，含关联工单、人员投入、异常记录
- 订单状态流转：开立 → 已下达 → 已关闭
- 操作权限：仅"开立"状态可修改/删除，删除前校验是否存在关联工单

**关键文件**：
- 前端：[src/pages/production/OrderManagement.jsx](file:///workspace/src/pages/production/OrderManagement.jsx)
- 后端：[server/src/controllers/OrderController.js](file:///workspace/server/src/controllers/OrderController.js)

#### 7.2.2 工单（WorkOrderManagement）

**核心功能**：
- 新增工单时选择关联生产订单后，自动显示订单剩余未完工数量（订单计划数量 - 已分配工单数量合计）
- 页面布局：
  - 第一行：关联生产订单（整行）
  - 第二行：料号 / 菲林编号 / 版本号（三列）
  - 第三行：产品名称（整行）
  - 剩余字段：一行两列排布
- 页面底部红色显示生产订单备注内容
- 已删除字段：班组长、备注
- 状态流转：开立 → 开工 → 完工 / 关闭

**关键文件**：
- 前端：[src/pages/production/WorkOrderManagement.jsx](file:///workspace/src/pages/production/WorkOrderManagement.jsx)
- 后端：[server/src/controllers/WorkOrderController.js](file:///workspace/server/src/controllers/WorkOrderController.js)

#### 7.2.3 其他生产页面

| 页面 | 路径 | 功能 |
|------|------|------|
| 工序报工 | /production/reporting | 工序完工数量、工时申报 |
| 按订单报工 | /production/reporting-by-order | 按订单维度汇总报工 |
| 人员投入 | /production/manpower | 工单人员投入记录 |
| 异常记录 | /production/exceptions | 生产异常申报与跟踪 |

### 7.3 质量管理模块

| 页面 | 路径 | 功能 |
|------|------|------|
| 检验标准 | /quality/standards | 检验项目与标准值定义 |
| 来料检验 | /quality/incoming | 原材料来料检验记录 |
| 工序检验 | /quality/process | 生产过程工序检验 |
| 成品检验 | /quality/finished | 成品出厂检验 |
| 微生物检验 | /quality/microbe | 微生物指标检测 |
| 环境检验 | /quality/environment | 生产环境卫生检测 |
| 客诉管理 | /quality/complaints | 客户投诉记录与处理 |
| 供应商客诉 | /quality/supplier | 供应商质量客诉 |
| 仪器管理 | /quality/instruments | 检测仪器台账与校准 |

### 7.4 设备管理模块

| 页面 | 路径 | 功能 |
|------|------|------|
| 设备档案 | /device/list | 设备基础信息台账 |
| 点检记录 | /device/check-records | 日常点检记录 |
| 维修保养 | /device/maintenance | 维修保养计划与记录 |
| 设备 OEE | /device/oee | 设备综合效率分析 |

### 7.5 报表中心模块

| 页面 | 路径 | 功能 |
|------|------|------|
| 日报 | /report/daily | 每日生产汇总 |
| 月报 | /report/monthly | 月度生产汇总 |
| 效率报表 | /report/efficiency | 生产效率分析 |
| 生产报表 | /report/production | 生产完成情况 |
| 质量报表 | /report/quality | 质量指标汇总 |
| 异常报表 | /report/exception | 异常事件统计 |

### 7.6 大屏展示模块

| 页面 | 路径 | 功能 |
|------|------|------|
| 生产大屏 | /bigscreen/production | 生产进度、产量实时展示 |
| 管理大屏 | /bigscreen/management | 综合管理指标看板 |
| 质量大屏 | /bigscreen/quality | 质量指标实时监控 |

大屏基于 ECharts 6 实现，使用独立全屏路由，不套用主布局。

### 7.7 系统管理模块

#### 7.7.1 用户管理（UserManagement）

- 用户 CRUD、启用/禁用切换
- 关联角色
- 重置密码

#### 7.7.2 角色管理（RoleManagement）

- 角色 CRUD
- 权限分配（通过角色-权限关联表）

#### 7.7.3 数据字典（DataDictionary）

**核心特性**：
- 动态读取数据库表结构，无需手动维护字典
- 页面打开自动更新数据
- 支持手动点击"更新"按钮刷新
- 默认每页显示 30 条
- 支持表名搜索与分类筛选
- 通过 `sqlite_master` 查询所有用户表，`PRAGMA table_info` 获取字段详情，`COUNT(*)` 获取记录数
- 按表名前缀（sys_ / bas_ / production_）自动分类

**关键文件**：
- 前端：[src/pages/system/DataDictionary.jsx](file:///workspace/src/pages/system/DataDictionary.jsx)
- 后端：[server/src/controllers/DataDictionaryController.js](file:///workspace/server/src/controllers/DataDictionaryController.js)

#### 7.7.4 系统配置（SystemConfig）

- 系统参数键值对配置
- 启动时自动初始化默认配置

#### 7.7.5 操作日志（OperationLogs）

- 记录用户操作（查询/新增/修改/删除）
- 包含用户、模块、操作、方法、参数、IP、时间

---

## 8. API 接口文档

### 8.1 接口约定

- **基础路径**：`/api`
- **认证方式**：`Authorization: Bearer <token>`（除登录外所有接口均需认证）
- **请求格式**：`application/json`
- **响应格式**：

```json
{
  "success": true,
  "message": "操作成功",
  "data": {},
  "total": 100
}
```

### 8.2 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 登录，返回 token 与用户信息 |
| GET | /api/auth/profile | 获取当前用户信息 |
| POST | /api/auth/logout | 退出登录 |

### 8.3 系统管理接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/system/users | 用户列表 |
| GET | /api/system/users/:id | 用户详情 |
| POST | /api/system/users | 新增用户 |
| PUT | /api/system/users/:id | 修改用户 |
| DELETE | /api/system/users/:id | 删除用户 |
| POST | /api/system/users/:id/toggle | 启用/禁用用户 |
| GET | /api/system/roles | 角色列表 |
| POST | /api/system/roles | 新增角色 |
| PUT | /api/system/roles/:id | 修改角色 |
| DELETE | /api/system/roles/:id | 删除角色 |
| GET | /api/system/permissions | 权限列表 |
| GET | /api/system/roles/:id/permissions | 角色权限 |
| PUT | /api/system/roles/:id/permissions | 分配权限 |
| GET | /api/system/logs | 操作日志 |
| GET | /api/system/config | 系统配置 |
| PUT | /api/system/config | 保存配置 |
| GET | /api/system/data-dictionary | 数据字典表列表 |
| GET | /api/system/data-dictionary/detail?name=表名 | 表字段详情 |

### 8.4 基础数据接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/basic/materials | 料品列表 |
| GET | /api/basic/materials/:id | 料品详情 |
| POST | /api/basic/materials | 新增料品 |
| PUT | /api/basic/materials/:id | 修改料品 |
| DELETE | /api/basic/materials/:id | 删除料品 |
| GET | /api/basic/production-lines | 产线列表 |
| GET | /api/basic/processes | 工序列表 |
| GET | /api/basic/devices | 设备列表 |
| GET | /api/basic/defect-types | 不良分类列表 |

（上述资源均支持标准 CRUD：GET 列表/详情、POST 新增、PUT 修改、DELETE 删除）

### 8.5 生产管理接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/production/orders | 订单列表（支持 keyword/materialCode/status/planDateStart/planDateEnd） |
| GET | /api/production/orders/:id | 订单详情（含工单、人员、异常） |
| POST | /api/production/orders | 新增订单（自动生成订单号） |
| PUT | /api/production/orders/:id | 修改订单 |
| DELETE | /api/production/orders/:id | 删除订单 |
| POST | /api/production/orders/:id/release | 下达订单 |
| POST | /api/production/orders/:id/close | 关闭订单 |
| GET | /api/production/work-orders | 工单列表 |
| GET | /api/production/work-orders/:id | 工单详情 |
| POST | /api/production/work-orders | 新增工单 |
| PUT | /api/production/work-orders/:id | 修改工单 |
| DELETE | /api/production/work-orders/:id | 删除工单 |
| POST | /api/production/work-orders/:id/start | 开工 |
| POST | /api/production/work-orders/:id/finish | 完工 |
| POST | /api/production/work-orders/:id/close | 关闭工单 |
| GET | /api/production/process-reports | 工序报工列表 |
| POST | /api/production/process-reports | 新增报工 |
| GET | /api/production/manpower-records | 人员投入列表 |
| POST | /api/production/manpower-records | 新增人员投入 |
| GET | /api/production/exceptions | 异常记录列表 |
| POST | /api/production/exceptions | 新增异常记录 |

---

## 9. 业务流程

### 9.1 生产订单业务流程

```
┌─────────┐    下达    ┌──────────┐    关闭    ┌──────────┐
│  开立   │ ─────────> │  已下达  │ ─────────> │  已关闭  │
│ status=0│            │ status=1 │            │ status=2 │
└─────────┘            └──────────┘            └──────────┘
     │                      │
     │ 可修改/删除           │ 可创建工单
     └──────────┘           └──────────┘
```

**业务规则**：
- 创建订单时自动生成订单号：`MO-16` + `YYMMDD` + `3位序号`
- 创建订单时关联料品，冗余料号/名称/规格/菲林编号/版本号
- 仅"开立"状态可修改、删除
- 删除前校验是否存在关联工单，存在则禁止删除
- "已下达"状态可创建工单、下达生产
- 状态流转：开立 → 已下达（记录 release_time）→ 已关闭（记录 close_time）

### 9.2 工单业务流程

```
┌─────────┐   开工    ┌─────────┐   完工    ┌─────────┐
│  开立   │ ────────> │  开工   │ ────────> │  完工   │
│ status=0│           │ status=1│           │ status=3│
└─────────┘           └─────────┘           └─────────┘
                            │
                            │ 关闭
                            v
                       ┌─────────┐
                       │  关闭   │
                       │ status=2│
                       └─────────┘
```

**业务规则**：
- 工单必须关联生产订单
- 新增工单时显示订单剩余未完工数量 = 订单计划数量 - 已分配工单目标数量合计
- 状态映射：开立=0 / 开工=1 / 关闭=2 / 完工=3
- 字段映射：前端 `planned_qty` ↔ 后端 `target_qty`，前端 `plan_start_time`/`plan_end_time` ↔ 后端 `start_time`/`finish_time`

### 9.3 工序报工流程

```
工单开工 → 工序报工（记录完工数量、工时）→ 工单完工
                ↓
        人员投入记录（人工工时）
        异常记录（生产异常）
```

### 9.4 用户认证流程

```
登录页 ─POST /api/auth/login─> 后端校验账号密码
                                   │
                                   ├─ 成功：返回 { token, user }
                                   │       前端存 localStorage
                                   │       跳转 /dashboard
                                   │
                                   └─ 失败：返回错误信息
```

- Token 有效期内，每次请求自动携带 `Authorization: Bearer <token>`
- 401 响应触发前端清除 token 并跳转登录页

---

## 10. 部署说明

### 10.1 开发环境部署

```bash
# 1. 克隆仓库
git clone https://github.com/gddsb/CSDM01.git
cd CSDM01

# 2. 安装后端依赖并启动
cd server
npm install
npm run dev      # 启动后端，监听 3001

# 3. 新开终端，安装前端依赖并启动
cd /workspace
npm install
npm run dev      # 启动前端，监听 5173

# 4. 访问应用
# 前端：http://localhost:5173
# 后端：http://localhost:3001/api
# 健康检查：http://localhost:3001/api/health
```

### 10.2 生产环境构建

```bash
# 前端构建
npm run build      # 产物在 dist/

# 后端启动（建议配合 pm2）
DB_DIALECT=mysql \
DB_HOST=xxx \
DB_PORT=3306 \
DB_NAME=milk_can_mes \
DB_USER=xxx \
DB_PASSWORD=xxx \
PORT=3001 \
npm run start
```

### 10.3 数据库初始化与种子数据

```bash
# 启动时自动建表（sync，仅创建不存在的表）
# 初始化默认系统配置、默认权限
node src/app.js

# 导入料品数据（114 条）
node src/import-materials.js

# 初始化种子数据（用户/角色等）
npm run seed

# 清空所有业务数据（保留用户/角色/权限）
node src/clean-init.js
```

---

## 11. 开发规范与注意事项

### 11.1 命名规范

| 范围 | 规范 | 示例 |
|------|------|------|
| 数据库表 | 蛇形，按模块前缀 | `sys_user` / `bas_material` / `production_order` |
| 数据库字段 | 蛇形 | `material_code` / `plan_start_time` |
| 后端模型 | PascalCase | `Order` / `WorkOrder` |
| 后端文件 | camelCase | `OrderController.js` |
| 前端组件 | PascalCase | `OrderManagement.jsx` |
| 前端路由 | kebab-case | `/production/work-orders` |
| API 路径 | kebab-case 复数 | `/api/production/work-orders` |

### 11.2 状态字段处理规范

**关键原则**：状态字段统一使用数字存储，通过 getter/setter 自动映射中文。

**注意事项**：
1. **更新状态时必须使用数字值**，避免 setter 转换问题：
   ```javascript
   await order.update({ status: 1 })  // ✅ 正确
   await order.update({ status: '已下达' })  // ⚠️ 不推荐
   ```
2. **前端筛选时状态值应使用数字**（0/1/2），避免 URL 编码导致中文解析失败
3. **0 值判断需使用显式判断**，不能使用 `if (status)`：
   ```javascript
   if (status !== undefined && status !== '')  // ✅ 正确
   if (status)  // ❌ 错误，0 会被判为 false
   ```

### 11.3 前后端字段映射

部分模块存在前端字段名与后端模型字段名不一致，后端控制器做了兼容处理：

| 前端字段 | 后端模型字段 | 涉及模块 |
|---------|-------------|---------|
| planned_qty | target_qty | 工单 |
| plan_start_time | start_time | 工单 |
| plan_end_time | finish_time | 工单 |
| film_no (Material) | film_version (Order) | 订单创建时映射 |

后端兼容写法：
```javascript
target_qty: req.body.planned_qty || req.body.target_qty,
start_time: req.body.plan_start_time || req.body.start_time,
finish_time: req.body.plan_end_time || req.body.finish_time,
```

### 11.4 已知问题与解决方案

#### 11.4.1 状态筛选结果不一致（闭包陷阱）

**问题**：状态下拉筛选时，`setTimeout(handleSearch, 0)` 中读取的 `statusInput` 为旧值。

**原因**：`setStatusInput(v)` 是异步更新，`setTimeout` 闭包捕获的是旧值。

**解决方案**：直接在 `onChange` 中用新值 `v` 设置 `query`，不依赖闭包中的 `statusInput`：
```javascript
<Select value={statusInput} onChange={(v) => {
  setStatusInput(v)
  setQuery(q => ({ ...q, page: 1, status: v }))
}} />
```

#### 11.4.2 数据库字典动态读取

通过 `sqlite_master` + `PRAGMA table_info` 动态查询表结构，无需手动维护字典数据。MySQL 环境下需替换为 `information_schema` 查询。

#### 11.4.3 Modal 销毁与表单重置

新增/编辑弹窗使用 `destroyOnHidden` 配合 Form 的 `preserve={false}`，确保关闭后表单状态完全重置，避免数据残留。

---

## 附录

### 附录 A：环境变量说明

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| PORT | 3001 | 后端监听端口 |
| DB_DIALECT | sqlite | 数据库类型（sqlite / mysql） |
| DB_HOST | localhost | MySQL 主机 |
| DB_PORT | 3306 | MySQL 端口 |
| DB_NAME | milk_can_mes | MySQL 数据库名 |
| DB_USER | root | MySQL 用户名 |
| DB_PASSWORD | | MySQL 密码 |
| JWT_SECRET | | JWT 签名密钥 |

### 附录 B：默认账号

种子数据初始化后默认账号（如有变更以 seed.js 为准）：
- 管理员：admin / admin123

### 附录 C：参考文件

- 前端入口：[src/main.jsx](file:///workspace/src/main.jsx)
- 后端入口：[server/src/app.js](file:///workspace/server/src/app.js)
- 路由聚合：[server/src/routes/index.js](file:///workspace/server/src/routes/index.js)
- 模型聚合：[server/src/models/index.js](file:///workspace/server/src/models/index.js)
- 数据库配置：[server/src/config/database.js](file:///workspace/server/src/config/database.js)
- HTTP 封装：[src/utils/api.js](file:///workspace/src/utils/api.js)
- 全局上下文：[src/contexts/AppContext.jsx](file:///workspace/src/contexts/AppContext.jsx)
- Vite 配置：[vite.config.js](file:///workspace/vite.config.js)

---

*文档结束*
